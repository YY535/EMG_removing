function varargout = SREaffineV(x,y,varargin)
% function varargout = SREaffineV(x,y,varargin) 

if nargin > 2
    iscenter = varargin{1};
else
    iscenter = true;
end
nt = length(y);
[nx, nch] = size(x);
if nx ~= nt
    if nt == nch
        x = x';
        nch = nx;
    else
        fprintf('size mismatch')
    end
end
for k = 1:nch
    tmp_x = [ones(nt, 1), ]
if iscenter 
    
end